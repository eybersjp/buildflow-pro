# Command: /build-feature

When this command is run, activate the Feature Build workflow.

Read: `.antigravity/workflows/07-feature-build.md`

Follow all steps in that workflow exactly.

Key requirements:
- Do not build without a feature brief first
- Do not skip tests
- Do not skip security review
- Produce a completion report
- Update project state after completion
